// Reusable helpers and shared logic live here (validation, utils, clients).
// zod is installed and ready to use for runtime validation. Example:
//
//   import { z } from 'zod'
//   export const ExampleSchema = z.object({ name: z.string() })

export {}
