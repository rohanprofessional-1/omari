import type { StepProps } from '../../../lib/reconcile/types'
import type { CutoffQuestion } from '../../../lib/reconcile/questions/cutoffs'

/** Placeholder — replaced by the cutoffs section build. */
export default function CutoffStep(_props: StepProps<CutoffQuestion>) {
  return null
}
