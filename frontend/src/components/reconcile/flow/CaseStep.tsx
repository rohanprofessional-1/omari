import type { StepProps } from '../../../lib/reconcile/types'
import type { CaseQuestion } from '../../../lib/reconcile/questions/cases'

/** Placeholder — replaced by the cases section build. */
export default function CaseStep(_props: StepProps<CaseQuestion>) {
  return null
}
