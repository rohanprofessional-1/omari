import type { StepProps } from '../../../lib/reconcile/types'
import type { ScopeQuestion } from '../../../lib/reconcile/questions/scope'

/** Placeholder — replaced by the scope section build. */
export default function ScopeStep(_props: StepProps<ScopeQuestion>) {
  return null
}
