import type { StepProps } from '../../../lib/reconcile/types'
import type { SafetyQuestion } from '../../../lib/reconcile/questions/safety'

/** Placeholder — replaced by the safety section build. */
export default function SafetyStep(_props: StepProps<SafetyQuestion>) {
  return null
}
