import type { StepProps } from '../../../lib/reconcile/types'
import type { RoutingQuestion } from '../../../lib/reconcile/questions/routing'

/** Placeholder — replaced by the routing section build. */
export default function RoutingStep(_props: StepProps<RoutingQuestion>) {
  return null
}
