import type { StepProps } from '../../../lib/reconcile/types'
import type { TestsQuestion } from '../../../lib/reconcile/questions/tests'

/** Placeholder — replaced by the tests section build. */
export default function TestsStep(_props: StepProps<TestsQuestion>) {
  return null
}
